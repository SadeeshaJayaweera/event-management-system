spring:
  application:
    name: admin-service
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
